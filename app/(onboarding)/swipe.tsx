import { View, Text } from "react-native";

export default function SwipeScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Style Swipe</Text>
    </View>
  );
}
